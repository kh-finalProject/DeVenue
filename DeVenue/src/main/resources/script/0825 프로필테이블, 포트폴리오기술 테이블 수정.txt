-- 프로필테이블 기본키의 컬럼명과 프로젝트테이블 기본키의 컬럼명이 같아서 변경

ALTER TABLE PROFILE RENAME COLUMN PRO_ID TO PROFILE_ID;
ALTER TABLE PROFILE RENAME COLUMN PRO_IMG TO PROFILE_IMG;

-- 포트폴리오테이블에 프로필ID컬럼이 외래키이기 때문에 같이 변경
ALTER TABLE PORTFOLIO RENAME COLUMN PRO_ID TO PROFILE_ID;

-- 파트너스 정보 테이블에 프로필ID컬럼이 외래키이기 때문에 같이 변경
ALTER TABLE PARTINFO RENAME COLUMN PRO_ID TO PROFILE_ID;

-- 포트폴리오기술(PORT_TEC)의 기본키가 없음 그래서 추가함
ALTER TABLE PORT_TEC ADD PT_ID NUMBER NOT NULL;
COMMENT ON COLUMN PORT_TEC.PT_ID IS '포트폴리오기술ID';
ALTER TABLE PORT_TEC DROP PRIMARY KEY;
ALTER TABLE PORT_TEC ADD CONSTRAINT PK_PORT_TEC PRIMARY KEY (
	PT_ID
);